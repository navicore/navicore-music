{
  "name": "navicore-music-deployment",
  "version": "1.0.0",
  "private": true,
  "description": "Deployment tooling for Navicore Music",
  "scripts": {
    "deploy": "./deploy-navicore-tech.sh",
    "deploy:api": "wrangler deploy",
    "deploy:pages": "wrangler pages deploy dist --project-name navicore-music",
    "dev:api": "wrangler dev",
    "setup": "npm install && ./setup-cloudflare.sh",
    "wrangler": "wrangler"
  },
  "devDependencies": {
    "wrangler": "^4.23.0"
  }
}
